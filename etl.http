### Run ETL — WAREHOUSE_DICT
POST {{host}}/api/etl/run
Content-Type: application/json

{
  "accountId": 1,
  "event": "WAREHOUSE_DICT",
  "burst": 1
}

> {%
  client.test("ETL WAREHOUSE_DICT — run", function () {
    client.assert(
        response.status === 200 || response.status === 202,
        "expected 200 or 202, got " + response.status
    );
  });
%}

### Run ETL — COMMISSION_DICT
POST {{host}}/api/etl/run
Content-Type: application/json

{
  "accountId": 1,
  "event": "COMMISSION_DICT",
  "burst": 1
}

> {%
  client.test("ETL COMMISSION_DICT — run", function () {
    client.assert(
        response.status === 200 || response.status === 202,
        "expected 200 or 202, got " + response.status
    );
  });
%}

### Run ETL — CATEGORY_DICT
POST {{host}}/api/etl/run
Content-Type: application/json

{
  "accountId": 1,
  "event": "CATEGORY_DICT",
  "burst": 1
}

> {%
  client.test("ETL CATEGORY_DICT — run", function () {
    client.assert(
        response.status === 200 || response.status === 202,
        "expected 200 or 202, got " + response.status
    );
  });
%}

### Run ETL — COMMISSION_DICT
POST {{host}}/api/etl/run
Content-Type: application/json

{
  "accountId": 1,
  "event": "COMMISSION_DICT",
  "burst": 1
}

> {%
  client.test("ETL COMMISSION_DICT — run", function () {
    client.assert(
        response.status === 200 || response.status === 202,
        "expected 200 or 202, got " + response.status
    );
  });
%}

### Run ETL — PRODUCT_DICT
POST {{host}}/api/etl/run
Content-Type: application/json

{
  "accountId": 1,
  "event": "PRODUCT_DICT",
  "burst": 1
}

> {%
  client.test("ETL PRODUCT_DICT — run", function () {
    client.assert(
        response.status === 200 || response.status === 202,
        "expected 200 or 202, got " + response.status
    );
  });
%}

### Run ETL — SALES_FACT
POST {{host}}/api/etl/run
Content-Type: application/json

{
  "accountId": 1,
  "event": "SALES_FACT",
  "burst": 1,
  "dateMode": "LAST_DAYS"
}

> {%
  client.test("ETL SALES_FACT — run", function () {
    client.assert(
        response.status === 200 || response.status === 202,
        "expected 200 or 202, got " + response.status
    );
  });
%}

### Run ETL — FACT_LOGISTICS_COSTS
POST {{host}}/api/etl/run
Content-Type: application/json

{
  "accountId": 1,
  "event": "FACT_LOGISTICS_COSTS",
  "burst": 1,
  "dateMode": "LAST_DAYS"
}

> {%
  client.test("ETL FACT_LOGISTICS_COSTS — run", function () {
    client.assert(
        response.status === 200 || response.status === 202,
        "expected 200 or 202, got " + response.status
    );
  });
%}

### Run ETL — INVENTORY_FACT
POST {{host}}/api/etl/run
Content-Type: application/json

{
  "accountId": 1,
  "event": "INVENTORY_FACT",
  "burst": 1,
  "dateMode": "LAST_DAYS"
}

> {%
  client.test("ETL SALES_FACT — run", function () {
    client.assert(
        response.status === 200 || response.status === 202,
        "expected 200 or 202, got " + response.status
    );
  });
%}
