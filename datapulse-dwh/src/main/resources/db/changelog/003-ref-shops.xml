<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
                   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                   xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                   https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.23.xsd">
  <changeSet id="003-ref-shops" author="vkim">
    <sql>
      create table if not exists ref.shops (
        id bigserial primary key,
        marketplace text not null, -- wb|ozon
        shop_id text,              -- ozon client-id
        token text not null,       -- wb authorization / ozon api-key
        enabled boolean not null default true
      );
      create index if not exists ix_ref_shops_enabled on ref.shops(enabled);
    </sql>
  </changeSet>
</databaseChangeLog>
