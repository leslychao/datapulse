<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
  xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="
        http://www.liquibase.org/xml/ns/dbchangelog
        https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.4.xsd">

  <changeSet id="0005-create-raw_warehouse_list_ozon" author="vitalii.kim" dbms="postgresql">

    <sql>
      -- ===========================================
      -- RAW WAREHOUSE LIST — OZON
      -- ===========================================
      CREATE TABLE raw_warehouse_list_ozon
      (
        id          BIGSERIAL PRIMARY KEY,
        request_id  VARCHAR(64) NOT NULL,
        snapshot_id VARCHAR(64) NOT NULL,
        account_id  BIGINT      NOT NULL,
        marketplace VARCHAR(32) NOT NULL,
        payload     JSONB       NOT NULL,
        created_at  TIMESTAMPTZ NOT NULL DEFAULT now()
      );

      CREATE INDEX IF NOT EXISTS idx_raw_warehouse_list_ozon_request
        ON raw_warehouse_list_ozon (request_id);

      CREATE INDEX IF NOT EXISTS idx_raw_warehouse_list_ozon_snapshot
        ON raw_warehouse_list_ozon (snapshot_id);

      CREATE INDEX IF NOT EXISTS idx_raw_warehouse_list_ozon_acc_mp
        ON raw_warehouse_list_ozon (account_id, marketplace);
    </sql>

    <rollback>
      DROP TABLE IF EXISTS raw_warehouse_list_ozon CASCADE;
    </rollback>

  </changeSet>

</databaseChangeLog>
