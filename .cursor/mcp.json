{
  "mcpServers": {
    "datapulse-postgres": {
      "command": "node",
      "args": ["${workspaceFolder}/scripts/mcp-postgres-launcher.mjs"],
      "cwd": "${workspaceFolder}"
    },
    "datapulse-clickhouse": {
      "command": "node",
      "args": ["${workspaceFolder}/scripts/mcp-clickhouse-launcher.mjs"],
      "cwd": "${workspaceFolder}"
    }
  }
}
